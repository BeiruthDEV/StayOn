/** Escala de espaçamento derivada dos valores usados no protótipo. */
export const spacing = {
  xxs: 2,
  xs: 4,
  sm: 6,
  md: 8,
  lg: 10,
  xl: 13,
  xxl: 16,
  /** Padding horizontal padrão das telas. */
  screen: 20,
  section: 24,
} as const;

/** Altura mínima de alvo de toque (recomendação de acessibilidade). */
export const touchTarget = 44;
