/** Conteúdo fixo da sessão de foco no protótipo. */

/** Objetivo declarado da sessão em andamento. */
export const sessionGoal = 'Finalizar a autenticação JWT';

/** Duração padrão quando não há bloco planejado em aberto (em minutos). */
export const defaultSessionMinutes = 45;

/** Interrupções registradas na sessão. */
export const interruptionCount = 0;

/** Tentativas de abrir aplicativos bloqueados durante a sessão. */
export const blockedAttempts = 2;
